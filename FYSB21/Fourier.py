import numpy as np
import matplotlib.pyplot as plt
import cmath
import scipy.optimize as scp

plt.rcParams['text.usetex'] = True

# sr = 2000   # sampling rate
# ts = 1/sr   # sampling interval
# t = np.arange(0, 1, ts)

# freq = np.random.rand(10,1)
# print(freq)

# x = 1   # first der of exp?

# for i in freq:
#     x += np.sin(2*np.pi*i*10*t)

# ax = plt.figure()
# plt.subplot(122)
# plt.plot(t, x, "r")



# X = np.fft.fft(x)   
# print(X)
# N = len(X)
# n = np.arange(N)
# T = N/sr
# freq = n/T 

# plt.subplot(121)
# plt.stem(freq, np.abs(X), "b", \
#          markerfmt=" ", basefmt="-b")
# plt.xlim(0,10)
# plt.show()

# plt.plot( t, np.fft.ifft(X), "g")   # inv3rse fft

# plt.show()







